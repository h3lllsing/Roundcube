<div class="sen-criteria" id="sen-criteria" aria-live="polite">
    <span>
        <strong>Sensitive modules</strong> — modules where
        <span class="tag">Delete</span>,
        <span class="tag">Reveal</span>, or
        <span class="tag">Import</span>
        permissions have elevated business impact and are excluded from all presets (require Custom):
    </span>
    <span class="text-amber-800 dark:text-amber-400">Domains (data loss), Hosting (data loss, credential exposure), VPS (data loss), Users (privilege escalation), API Tokens (credential exposure)</span>
</div>
